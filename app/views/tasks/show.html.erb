<h1><%= @task.title %></h1>
<h2><%= @task.details %></h2>

<%= link_to "go back", tasks_path %>

<%= link_to "edit task", task_edit_path(@task.id) %>

<%= link_to "Delete", task_destroy_path(@task.id),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
