<h1>All Tasks</h1>
<p>Here are my tasks:</p>
<ul>
  <% @tasks.each do |task| %>
  <li>
    <h2>
      <%= link_to task.title, task_path(task) %> - <%= task.completed %>
    </h2>
  </li>
  <% end %>
</ul>

